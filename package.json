{
    "name": "shahae-spend-fullstack",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "cd backend && npm install && cd ../frontend && npm install && npm run build",
        "start": "node backend/server.js"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}